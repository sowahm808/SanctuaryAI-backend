import {ArgumentsHost,Catch,ExceptionFilter,HttpException,HttpStatus} from '@nestjs/common'; import type {Response} from 'express'; import {requestContext} from './request-context';
@Catch() export class ProblemFilter implements ExceptionFilter {catch(exception:unknown,host:ArgumentsHost){const response=host.switchToHttp().getResponse<Response>(); const status=exception instanceof HttpException?exception.getStatus():HttpStatus.INTERNAL_SERVER_ERROR; const value=exception instanceof HttpException?exception.getResponse():'An unexpected error occurred'; const detail=typeof value==='string'?value:'Request could not be processed'; response.status(status).type('application/problem+json').json({type:`https://sanctuaryai.app/errors/${status}`,title:status===422?'Validation failed':HttpStatus[status]??'Error',status,detail,correlationId:requestContext.getStore()?.correlationId});}}
