import {ConflictException,Injectable,NotFoundException} from '@nestjs/common'; import {PrismaService} from '../../database/prisma.service';
export interface CampaignInput {month:number;year:number;title:string;numberOfSundays:number;createdBy:string}
@Injectable() export class CampaignsService {constructor(private readonly db:PrismaService){} async create(organizationId:string,input:CampaignInput){try{return await this.db.monthlyCampaign.create({data:{...input,organizationId}});}catch{throw new ConflictException('A campaign already exists for this month');}} async findOne(organizationId:string,id:string){const item=await this.db.monthlyCampaign.findFirst({where:{id,organizationId,archivedAt:null}}); if(!item) throw new NotFoundException('Campaign not found'); return item;} list(organizationId:string){return this.db.monthlyCampaign.findMany({where:{organizationId,archivedAt:null},orderBy:[{year:'desc'},{month:'desc'}]});}}
